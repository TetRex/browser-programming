## Features Implemented
- A) Theme toggle
- B) Click counter

## How to Test
- Click the "Toggle Theme" button in the header to switch between light and dark modes.
- Click the "Click Me" button in the footer to increment the click count (check console for logs).
